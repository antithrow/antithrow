# antithrow core API audit — coverage report

> Part of the [API audit](../API_AUDIT.md). What each audit dimension examined and how it tested it.
> Findings are grouped by audit dimension. Repro scripts referenced in evidence lived in the session scratchpad (ephemeral); all key observed output is quoted inline. The full untruncated register is in [findings.json](./findings.json).

## ok-runtime

Audited the runtime semantics of `Ok` (/home/user/antithrow/packages/antithrow/src/ok.ts) against the contract published in base.ts JSDoc, types.ts, errors.ts, utils.ts, and the reference docs in apps/docs/docs/reference/antithrow/ok.md. Read err.ts, pending.ts and result.ts for cross-class comparison, and skimmed ok.test.ts to keep findings novel (existing tests cover map/mapOr/mapOrElse/andThen/and/or/orElse/flatten/unwrap*/settle happy paths and a single `Symbol.iterator` case; none cover freezing, foreign thenables, dual-package instanceof, unhandled rejections, serialization, or instance aliasing).

Eight runtime scripts + three tsc typecheck files, all left in place under /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/ok-runtime/:
- a-mutability.ts — property descriptors, Object.freeze/isSealed, direct write / Object.assign / delete on `value`, instance identity across mapErr/orElse/flatten/and/or/andThen, callback-invocation counting, settle() identity, Ok(1) equality.
- b-map.ts — map with: sync fn, fn returning undefined, fn returning Ok, fn returning Err, fn returning a Pending (that settles to Err), async fn, identity map over Ok<Promise<T>>, a well-behaved custom thenable, a thenable whose `then` returns undefined, a thenable whose `then` throws, a function carrying a `then`, and an object whose `then` is a non-function.
- c-broken-pending.ts — the `new Pending(undefined)` failure mode and what `settle()` returns for it.
- d-unhandled.ts / d2-unhandled-nohandler.mjs — sync-throw vs async-throw divergence; run under bun (with a handler) and under Node 22.22.2 (no handler, observed process exit 1).
- e-unwrap-iter-serialize.ts — UnwrapError contents (name/message/result/cause/stack/own keys/JSON/cyclic), unwrapOr/unwrapOrElse arg-ignoring, for..of / spread / Array.from / destructuring / raw iterator / yield*, asyncIterator absence, String/util.inspect/JSON.stringify/structuredClone/spread/Object.keys, zero-arg construction, detached-method binding, instanceof and prototype chain.
- f-dual-package.ts — two independent copies of the built package (dist + a byte copy), exercising `flatten()`'s instanceof checks across the realm boundary, including a nested foreign Err.
- g-misc.ts — map allocation identity, mapOr thenable pass-through, mapOrElse Ok/Err sync-vs-async asymmetry, settle() microtask ordering, subclass preservation across combinators, rejecting-Pending propagation, cyclic Ok.
- typecheck.ts / typecheck2.ts / typecheck3.ts — 30+ `Equal`/`Expect` assertions plus `@ts-expect-error` markers, all run with the repo's TypeScript 6.0.3 via `cd /home/user/antithrow && bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022 <file>` (all three exit 0). Note `--ignoreConfig` is required when passing files on the command line under TS 6.

Two type-level hypotheses were tested and DISPROVEN, so they are not reported as findings: (1) destructuring `const [first] = ok` does not type as `never` — TypeScript correctly infers `undefined` and rejects assigning it to `string`; (2) `Ok.map` on a nested-promise thenable does not lie about the value type. No files under /home/user/antithrow were modified, created, or deleted.

## err-runtime

Audited the runtime semantics of `Err` (/home/user/antithrow/packages/antithrow/src/err.ts) against base.ts, ok.ts, pending.ts, result.ts, types.ts, errors.ts, utils.ts, and the published reference page /home/user/antithrow/apps/docs/docs/reference/antithrow/err.md.

Method: every claim was executed. Runtime probes were run with `bun /abs/path.ts` importing the source directly from /home/user/antithrow/packages/antithrow/src/index.ts; type-level claims were checked with the repo's TypeScript via `bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom <file>`, and inferred types were read out by emitting declarations (`--declaration --emitDeclarationOnly --outDir …`) rather than guessed. `@ts-expect-error` markers were used to prove specific calls are rejected (tsc exit 0 = marker consumed). Nothing under /home/user/antithrow was modified.

Surface covered: constructor defaults and the zero-arg case; the SAFETY identity casts in `map`/`andThen`/`and`/`flatten` (reference identity, callback non-invocation, shared-instance aliasing, runtime mutability of `readonly error`); `mapErr` with sync fn, async fn, a rejecting async fn, a fn returning `Ok`/`Err`, a fn returning `Pending`, a well-formed custom thenable, a malformed thenable (`then` returning void), a chainable non-promise object with a `then` method, and `{ then: 42 }`; `mapOr` argument-evaluation order and the eager default; `mapOrElse` call ordering and mixed sync/async callback types; `or`/`orElse` including async recovery via `Result.try`; `unwrap` (UnwrapError name/message/stack/`cause`/`.result`/enumerability/JSON shape/`instanceof`); `unwrapErr`; `unwrapOr` and both `unwrapOrElse` overloads at `T = never` and on the `Result` union; `settle()` identity and allocation; `Symbol.iterator` under spread, `Array.from`, `for..of` (full drain and with `break`), 1- and 2-element destructuring, `new Map(...)`, `Promise.all(...)`, manual `next()`/`return()`/`throw()`, and inside `Result.do` vs. a hand-written generator; subclassing `Err` and `Ok` across every combinator; `instanceof` behaviour for subclasses and for a foreign/duplicated copy of the class; and serialization (`JSON.stringify`, `structuredClone`, `Symbol.toStringTag`, freezing).

Reproduction: all scripts are left in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/err-runtime/ and a single driver reruns everything (runtime probes, tsc diagnostic captures, and declaration emission) via `sh /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/err-runtime/run-all.sh`.

Deliberately out of scope for this dimension and left to other auditors: `Pending`'s own semantics, `Result.do`/`Result.try`/`Result.fromPromise` beyond their interaction with `Err`, and the `antithrow/legacy` subpath.

## pending-runtime

Dimension: runtime semantics of the `Pending` class (packages/antithrow/src/pending.ts) in antithrow v3.0.0.

Read: base.ts, ok.ts, err.ts, pending.ts, result.ts, types.ts, errors.ts, utils.ts, index.ts, pending.test.ts, plus docs (apps/docs/docs/reference/antithrow/pending.md, explanation/eager-vs-lazy.md, tutorial/04-go-async.md).

Empirical work: 14 scratch scripts under /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/pending-runtime/, all executed. Runtime under `bun 1.3.11` against src/*.ts, and cross-checked against the published `dist/` under `node v22.22.2` for the process-crash findings. Type-level claims checked with the repo's TypeScript 6.0.3 via `bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2023,dom --skipLibCheck <file>`, using `Equal`/`Expect` helpers and `@ts-expect-error` markers (07, 13), and an "assign to an impossible literal type" trick to print actual inferred types (08).

Areas exercised: await/then with and without handlers and with null handlers; thenable assimilation (Pending inside Pending, Ok inside Pending, thenable domain objects); hand-built Pending over a raw rejecting promise across every combinator (map/mapErr/mapOr/mapOrElse/andThen/and/or/orElse/flatten/unwrap/unwrapErr/unwrapOr/unwrapOrElse/settle); throwing and rejecting callbacks on the Pending path; unhandled-rejection accounting via process.on("unhandledRejection")/("rejectionHandled") and exit codes with no handler installed; multiple awaits and multiple .then on one Pending; branch sharing and inner-promise single-evaluation; map returning Pending/Result/thenable and the SAFETY flattening casts; mapOr/mapOrElse PromiseLike returns; unwrap on Pending-of-Err; settle() identity and .promise mutability; isPending narrowing; Symbol.asyncIterator used directly (for await, Array.fromAsync, manual iterator, Result.do integration incl. finally cleanup); eagerness/laziness and microtask depth; flatten of nested and poisoned Pendings; Pending crossing a Promise boundary.

Not covered (other dimensions): Ok/Err sync semantics except where needed as a control, Result.do beyond its Pending interaction, the antithrow/legacy subpath, and the @antithrow/std, node, standard-schema and eslint-plugin packages.

## constructors

Dimension: `Result.try` / `Result.fromPromise` in /home/user/antithrow/packages/antithrow/src/result.ts, plus `isThenable` in utils.ts, `NonThenable`/`SyncOrAsync` in types.ts, the emitted /home/user/antithrow/packages/antithrow/dist/result.d.ts, the existing suite at src/result.test.ts (lines 1104-1200), and the corresponding docs (apps/docs/docs/reference/antithrow/result.md, apps/docs/docs/how-to/core/wrap-a-throwing-function.md, apps/docs/docs/how-to/core/convert-a-promise.md).

Empirical method — all scripts left in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/factories/:
- Runtime, executed with `bun <abs path>` against the SOURCE (import from /home/user/antithrow/packages/antithrow/src/index.ts): runtime1.ts, runtime2.ts, runtime3.ts, silent-lie.ts, unknown-thenable.ts, fix-check.ts.
- Type level, executed from /home/user/antithrow with the repo's TypeScript 6.0.3: `bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom <abs path>` (--ignoreConfig required, otherwise TS5112). Files: typecheck.ts and typecheck2.ts (Equal/Expect assertions + @ts-expect-error markers, both exit 0), reveal.ts / reveal2.ts / reveal3.ts / doc-claims.ts (assign-to-`never` "reveal" trick so tsc prints the actual inferred type).

Cases exercised: sync value / sync throw (Error, string, undefined, null, 0) / promise resolve / promise reject / async fn throw; custom thenables (then returning undefined, then returning an unrelated promise, then throwing synchronously, then never settling); a callable function carrying `.then`; a class instance with a domain method named `then`; a `Proxy` whose every property is a function; `Object.create(null)` with `then`; an object with a throwing `then` getter; null/undefined/void returns; `Result.try(undefined as any)`; explicit vs inferred T and E; default E in both factories; all three `resultTry` overloads incl. `() => number | Promise<number>` and `() => Promise<number> | Promise<string>`; generic forwarding `function wrap<T>(fn: () => T)`; contextual-return-type inference of E; `fromPromise` on already-settled/rejecting/thenable/Pending/Promise<Promise<T>>; arity checks; unhandled-rejection behaviour; multi-await idempotence; and a validated one-line fix (`Promise.resolve(p).then(...)`) benchmarked against current behaviour across all five thenable shapes.

## do-notation

Audited `Result.do` (packages/antithrow/src/result.ts `resultDo`, lines 48-113) together with its iterator plumbing: `Ok[Symbol.iterator]` (ok.ts:131-134), `Err[Symbol.iterator]` (err.ts:121-124), `Pending[Symbol.asyncIterator]` (pending.ts:130-132), plus the `Settled`/`InferErr` types it depends on. Also read base.ts JSDoc (the `isPending` example there — `Result.try(async () => 42)` narrowed with `isPending()` then awaited — checked and found accurate, no finding), index.ts exports, and every `Result.do` mention across README.md, apps/docs reference/how-to pages, and packages/std's `fetch`/`encodeURIComponent` return types.

Empirical work (all scripts left in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/result-do/):
- Runtime batteries `01-runtime.ts`, `02-runtime2.ts`, `06-runtime3.ts`, `11-extra.ts`, and a labelled per-finding repro `10-findings-repro.ts`, all run with `bun <path>` against `packages/antithrow/src/index.ts`. Covered: sync/async happy paths; fail-fast with multiple Errs (only the first runs — confirmed via an ordering trace); `finally` on both paths, including `finally { return X }` (does NOT swallow the Err), `finally` that throws, `finally` that yields, and `finally` on the success path; `yield` vs `yield*`; `yield 42`; returning a Result / a Promise from the body; throwing in sync vs async bodies; `yield*` of a Pending in a sync generator; `yield*` of a Promise; `yield*` over `Ok`/`Err`/`Settled`/`Pending`/full `Result` unions in async generators (all four combinations); nested sync-in-sync, async-in-async and cross nesting; 1k/10k/100k-iteration loops (linear, 100k in ~26-31 ms); 1000-deep recursive `do` nesting; reuse of the same `Ok`/`Err`/`Pending` instance across blocks; manual `.next()`/`.return()` inspection of the generator's post-short-circuit state; `for..of`/`Array.from`/spread over `Ok` and `Err`.
- Process-level check with real Node: `node /tmp/.../07-node-crash.mjs; echo EXIT=$?` against `packages/antithrow/dist/index.js` (Node v22.22.2) to confirm the unhandled-rejection process termination in od-1.
- Type-level checks `03-reveal.ts`, `04-negative.ts`, `05-compose.ts`, `08-reveal2.ts`, `09-narrowing.ts`, each run as `cd /home/user/antithrow && bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022 <abs path>` with the repo's TypeScript 6.0.3 (`--ignoreConfig` was required; without it tsc 6 emits TS5112 when files are passed on the command line). Inferred types were revealed by assigning to `0` so tsc prints the actual type. Verified: Err-union inference (`Settled<number, "a" | "b">`), `Ok`'s declared E being dropped to `never`, `yield*`-on-`Err` typing as `never`, `yield*`-on-`Settled`/`Pending` typing as the value, the four overloads' returns matching the documented table, overload rejection of sync generators yielding non-Err values, and control-flow-narrowing behaviour vs a `never`-returning call.

Not covered (out of dimension): the `antithrow/legacy` subpath's `chain`, and the `this is`-predicate narrowing weirdness where `isPending()` on a `Settled` do-result compiles and narrows to the impossible intersection `Settled<number,"a"> & Pending<number,"a">` rather than `never` (observed in 08-reveal2.ts line 50, but it is a `ResultBase` predicate issue, not a `Result.do` one).

## types-overloads

Type-level audit of every public method overload on `Ok<T,E>`, `Err<T,E>`, `Pending<T,E>`, the `Result<T,E>` union, and the abstract `ResultBase<T,E>`, for: map, mapErr, mapOr, mapOrElse, andThen, and, or, orElse, unwrap, unwrapErr, unwrapOr, unwrapOrElse, settle, flatten.

Method: wrote TypeScript files that (a) reveal every inferred return type by assigning the expression to `never` and reading the `TS2322: Type 'X' is not assignable to type 'never'` diagnostic, and (b) lock the revealed types in with `Equal`/`Expect` assertions and `@ts-expect-error` markers so the whole claim set is a single pass/fail. Everything was compiled with the repo's own TypeScript 6.0.3 via:
  /home/user/antithrow/node_modules/.bin/tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom --noErrorTruncation --skipLibCheck <file>
(wrapped as /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/type-overloads/run-tsc.sh; `tsc --version` => 6.0.3).

Two consolidated gate files both exit 0, pinning ~50 exact type claims:
  - assertions.ts  (positive: 35 `Expect<Equal<...>>` assertions of the exact inferred type per receiver x method x sync/async/union callback)
  - negative.ts    (negative: 16 `@ts-expect-error` markers, all of which fire)

Runtime cross-checks were run with `bun` against the same source (runtime1.ts, unsound.ts, unsound2.ts) to compare the static type against the actual constructed class (`instanceof Ok/Err/Pending`), including heterogeneous sync/async chains, mixed-branch `mapOrElse`, `Err.map(async)`, `Ok.unwrapOrElse(async)`, and `or`'s error discarding.

Also probed: calls through the abstract `ResultBase` type, calls inside generic `<T,E>` helper functions, distributive-conditional behaviour on `boolean`/union/`never`/`any` callback returns, `NoInfer` placement on `mapOr`, `SameResolved` enforcement and its overload-3 escape hatch, `Pending`'s missing default type parameters, `PromiseLike` vs `Promise` on `settle()`, and the `@example` blocks in base.ts transcribed verbatim (docs.ts).

Two candidate replacement return types for `map` were prototyped and type-checked (fixproto.ts exit 0, fixproto2.ts exit 0) so the recommendations are demonstrated, not guessed.

## types-guards-variance

Audited type guards, narrowing and variance for antithrow core v3.0.0 by writing and running 20 scratch files under /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/guards-variance (see run-all.sh there for the exact command set).

Type-level checks were run with the repo's TypeScript 6.0.3 via `cd /home/user/antithrow && bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022 <file>` (TS 6 requires `--ignoreConfig` when files are passed on the command line). Assertions used a local `Equal`/`Expect` pair plus `@ts-expect-error` markers; actual inferred types were revealed by assigning to `0` and reading the TS2322 message. Runtime checks were run with `bun <file>` importing the source directly from /home/user/antithrow/packages/antithrow/src/index.ts.

What was examined: (1) narrowing of `Result<T,E>` through `isOk`/`isErr`/`isPending` in if/else-if/else chains, negated form, aliased conditions, dotted names, closures, generic constraints, and full `const _: never` exhaustiveness; (2) parity between the method guards, `instanceof`, and the `in` operator; (3) dead-branch narrowing on `Settled` and on statically-known `Ok`/`Err`/`Pending`; (4) array `filter`/`find`/`every` narrowing with inferred type predicates in positive, negated and compound forms; (5) variance — covariant widening of both parameters across `Ok`, `Err`, `Pending`, `Result`, `Settled`, rejection of the contravariant direction, rejection of phantom-parameter bivariance, and whether tsc verifies vs trusts the `out` annotations (it verifies, emitting TS2636, but its structural measurement is blind to conditional return types); (6) two exploit attempts that succeeded — `flatten()` and `Ok#map`'s conditional overload — each demonstrated with a program that type-checks at zero errors and produces a value contradicting its static type at runtime, including a binding typed `never` holding a string; (7) default type parameters `Ok<T, E = never>` / `Err<T = never, E = unknown>` / `Pending<T, E>` and their effect on `new Err<string>(...)`; (8) `Settled`↔`Result` assignability both directions; (9) `InferOk`/`InferErr` on unions, `any`, `never`, non-Results, intersections, and round-tripping; (10) the published type surface, checked from a synthetic consumer package resolving `antithrow` through the real `exports` map against dist/*.d.ts; (11) runtime guard/`instanceof` parity across all five construction paths and across two distinct copies of the package (src vs dist).

## flatten

Audited `flatten()` on Ok/Err/Pending plus the `FlattenOk`/`FlattenErr`/`FlattenPending` type functions in /home/user/antithrow/packages/antithrow/src/{base,ok,err,pending,types}.ts, cross-checked against the published dist .d.ts, the legacy subpath (src/legacy/result.ts), the existing test suite (ok.test.ts:320, err.test.ts:298, pending.test.ts:586, result.test.ts:558) and the docs (apps/docs/docs/how-to/core/combine-results.md, reference/antithrow/methods.md).

Method: every type claim was produced by a `declare function reveal<T>(x: T): T` probe assigned to a string-literal type, so tsc's TS2322 message prints the exact inferred type; absence of an error proves the type is `never`. Compiler: repo TypeScript 6.0.3, invoked as `cd /home/user/antithrow && bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --skipLibCheck <file>` (`--ignoreConfig` is required by 6.0.3 when files are passed on the CLI; `--allowImportingTsExtensions` dropped for the dist-typed probe). Every runtime claim was produced by `bun <file>.ts` against the source via absolute path.

Cases exercised: Ok(Ok), Ok(Err), Ok(Pending) (incl. object identity), Ok(plainValue), Ok(rawPromise), Ok of union T, Ok of `unknown`/`object`/`any`; Err(anything) incl. Err with a Result-typed phantom T and `.error`/`.unwrapErr()` access; Pending resolving to each of Ok(Ok), Ok(Err), Ok(Pending), Ok(Ok(Pending)), Ok(rawPromise), Err, and a 3-deep Pending chain (to measure thenable-assimilation collapse depth); inner-Pending rejection propagation; double and triple flatten on deeply nested values; flatten on the `Result<T,E>` union and on non-nested results; flatten behind generic type parameters; dual-module-instance (src vs dist) `instanceof` behaviour; and `andThen(identity)` vs `flatten()` equivalence. Repro scripts left at /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/flatten/ (reveal2.ts, reveal3.ts, reveal4.ts, reveal5_dist.ts, reveal6_andthen.ts, runtime1.ts, runtime2.ts, runtime3.ts). runtime2.ts and runtime3.ts both typecheck clean under the strict flags above, proving their traps are reachable from TS-accepted code.

## api-completeness

Audited the full public surface of `antithrow@3.0.0` (`packages/antithrow/src/{index,base,ok,err,pending,result,types,errors,utils}.ts`) against Rust `std::result::Result`, neverthrow, ts-results-es, oxide.ts, and Effect (`Either`/`Exit`), plus the shipped docs under `/home/user/antithrow/apps/docs/docs/` and the `antithrow/legacy` (v2) subpath for regression comparison.

Method: 29 repro scripts under `/tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/api-completeness/`. Runtime behaviour executed with `bun <script>.ts`. Type-level claims checked with the repo's TypeScript 6.0.3 via:
`cd /home/user/antithrow && bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom <file>`
(the bare command in the task brief fails with `TS5112`; `--ignoreConfig` is required on TS 6.0.3). Absence of a method was proven with `@ts-expect-error` markers that must fire for the file to compile at exit 0, plus runtime prototype enumeration. Inferred types that were not obvious were extracted with a `const reveal: { __reveal: true } = x` probe that prints the real type in the TS2322 message.

Covered: full instance-method inventory across `Ok`/`Err`/`Pending`; `Result` namespace statics; collection combinators and concurrency behaviour (timed); inspection combinators; `match`/`mapOrElse` argument order (runtime inversion demonstrated); error-mapper boundary for `Result.try`/`Result.fromPromise`; `fromThrowable` wrap-and-reuse; constructor/factory ergonomics and point-free usage; `isResult`/`instanceof`; `expect`/`expectErr` and `UnwrapError` message quality; Option-bridge converters; `swap`/`zip`/`zipWith`/`andThrough`/`orTee`; async-callback support asymmetry between `map` and `andThen`; serialization (`toString`/`toJSON`/`structuredClone`); and compilability of the core how-to doc examples.

Not covered (other dimensions): variance/soundness of the `out T, out E` annotations, `Result.do` generator cleanup semantics, `Symbol.iterator` protocol details, the `@antithrow/std`, `node`, `standard-schema`, and `eslint-plugin` packages.

## docs-accuracy

Audited every prose claim and @example block in the core package's doc surface, plus the package README and every core-API page on the docs site. Sources read in full: packages/antithrow/src/{base,ok,err,pending,result,types,errors,utils,index}.ts; packages/antithrow/README.md; apps/docs/docs/intro.md; apps/docs/docs/reference/antithrow/{result,ok,err,pending,settled,methods,unwrap-error}.md; apps/docs/docs/how-to/core/*.md (7 files); apps/docs/docs/tutorial/*.md (5 files); apps/docs/docs/explanation/*.md (7 files). std/node/schema pages ignored per scope.

Method: every @example and code fence was transcribed verbatim into a runnable bun script or a tsc-checked .ts file, then executed. Type claims written as doc comments ("mapped is Ok<number, string>") were encoded as `Expect<Equal<actual, claimed>>` assertions so a tsc error proves the doc wrong; actual inferred types were then revealed by deliberately-wrong assignments. Doc-coverage claims (what a user sees on hover) were measured with the repo's own TypeScript 6.0.3 language service via `getQuickInfoAtPosition`, inspecting `documentation` and `tags` and the `hasTarget` flag on {@link} parts.

Exact commands used:
- Runtime: `bun /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/docs-accuracy/<file>.ts`
- Types: `cd /home/user/antithrow && bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom <abs path>` (--ignoreConfig is required; without it tsc 6.0.3 refuses with TS5112 because the repo tsconfig.json is present)
- Hover/link probes: `node .../14-link-resolution.mjs`, `.../15-hover-coverage.mjs`, `.../18-throws-tags.mjs`

Coverage of the specific suspects named in the brief: mapOr "becomes PromiseLike" phrasing (checked — the phrasing itself is correct, but the conditional is wrong, see ok-8); orElse "recovered is Ok<number, string>" (wrong, ok-4); isPending example (compiles correctly, see praise); Result.do JSDoc types (Err<never,"invalid"> claims are correct; the surrounding prose is not, ok-9); errors.ts UnwrapError `Settled<unknown, unknown>` (exactly correct); README quick-start and all pipelines (all compile); every docs-site core code fence (compiled individually). Doc coverage assessed for and/or/settle (inherited base JSDoc — fine), Pending.then/Pending.promise, and the Symbol.iterator protocol.

All 18 repro scripts are left in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/docs-accuracy/. Not covered: the antithrow/legacy subpath docs, apps/docs legacy/ pages, and std/node/eslint-plugin/standard-schema reference pages.

## consumers

Read the full core surface (packages/antithrow/src/{base,ok,err,pending,result,types,errors,utils,index}.ts) and every production file plus tests in the four consuming packages: packages/std/src (7 modules), packages/node/src (fs/promises x7 + os), packages/standard-schema/src, packages/eslint-plugin/src (3 rules + result-type.ts detection util). Also read apps/docs (tutorial 04, explanation/{three-state-model,error-typing-philosophy,eager-vs-lazy}, how-to/core/{wrap-a-throwing-function,combine-results}) and knip.json/root lint pipeline.

Empirical work, all under /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/real-consumer/ (nothing in the repo was modified):
- 17 numbered .ts repros. Runtime via `bun <file>`; type-level via `bun x tsc --ignoreConfig --noEmit --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --lib es2022,dom [--typeRoots /home/user/antithrow/packages/node/node_modules/@types --types node]` using the repo's TypeScript 6.0.3, with self-defined Equal/Expect helpers and @ts-expect-error markers.
- lintlab/: a real eslint 10.6.0 + @typescript-eslint 8.63.0 project (installed into the scratchpad) with @antithrow/eslint-plugin, @antithrow/std, @antithrow/node and antithrow symlinked in. Used to compare no-floating-promises / await-thenable / no-misused-promises against no-unused-result, and to probe no-unused-result coverage gaps (src/floating.ts, src/awaiting.ts, src/gaps.ts).
- selfcheck/: copies of std/node/standard-schema sources linted with the plugin's own recommended rules (228 violations catalogued and attributed).
- fp/antithrow/ and fn/app/: fixtures proving the path-substring type detection both false-positives (with a destructive --fix) and false-negatives (rolled-up .d.ts).
- dual/: a simulated nested-peer install with two copies of antithrow, showing flatten() corruption under both bun and tsc.

Cross-realm behaviour was probed for instanceof, flatten, isOk duck-typing, and the sync/async iterator protocols. Consumer API usage was catalogued by grep across all 28 core symbols, separately for production source and tests. Not covered (out of dimension): the antithrow/legacy subpath internals, the core's own test suite quality, Docusaurus site build, and browser/Deno runtime behaviour (all runtime checks were on bun 1.3.11).

## errors-exceptions

Audited the exception posture / failure modes of antithrow core v3.0.0 by reading all of /home/user/antithrow/packages/antithrow/src/ (base.ts, ok.ts, err.ts, pending.ts, result.ts, types.ts, errors.ts, utils.ts, index.ts) plus the reference/tutorial docs under /home/user/antithrow/apps/docs/docs/. Nine repro scripts were written and executed in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/exception-posture/.

Runtime coverage (bun 'bun <script>.ts' against src/index.ts, and node v22.22.2 against dist/index.js): throwing sync callbacks on Ok.map/mapOr; throwing callbacks on the Pending path for map/mapErr/andThen/orElse/mapOrElse/unwrapOrElse; async callbacks that reject; poisoned Pendings flowing into Result.do via yield* on both sync and async generators; process-level 'unhandledRejection' listeners on every shape that allocates a promise (Ok.settle, Err.settle, Err.mapErr(async), Ok.map(async)); a real Node process-exit test; UnwrapError message/name/cause/stack/.result inspection; thenable and rejected-promise payloads as both T and E (assimilation, side-effecting thenables, thenables whose .then() throws); Result.fromPromise with a promise as the rejection reason; JSON.stringify and structuredClone of Ok/Err/Pending/UnwrapError.

Type-level coverage: 10 Equal/Expect assertions plus 2 @ts-expect-error markers in 06-typecheck.ts, verified with the repo's own TypeScript 6.0.3 via:
  cd /home/user/antithrow && bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --skipLibCheck /tmp/.../06-typecheck.ts
  -> EXIT=0 (all assertions hold, including the negative @ts-expect-error ones).

Also grepped the existing 534-test suite to confirm which of these paths are untested.

## packaging

Audited the packaging/exports/distribution surface of /home/user/antithrow/packages/antithrow @ 3.0.0 (repo left byte-for-byte unmodified; `git status --porcelain` empty after all work). All scratch artifacts live under /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/packaging/ and every claim below is reproducible via the single driver script `run-all.sh` there.

Read: package.json, tsconfig.json, tsconfig.build.json, README.md, CHANGELOG.md, all 9 dist/*.js, all 9 dist/*.d.ts, all dist/legacy/*, src/index.ts, src/types.ts, plus the four sibling workspace packages' package.json for consistency.

Empirically executed:
- CJS consumption: `require('antithrow')`, `require('antithrow/legacy')`, `require(<abs dist path>)` and dynamic `import()` from .cjs, under BOTH node v22.22.2 and bun 1.3.11 (scratchpad/consumer/).
- Counter-experiment: an identical copy of the package with only `"default"` added to each exports entry (scratchpad/patched/) to isolate cause from ESM-ness.
- TypeScript consumers built with the repo's own tsc 6.0.3 under `module: node16`, `nodenext`, `node20` (CJS package, scratchpad/cjs-ts/), `moduleResolution: node10` (scratchpad/node10/), and `nodenext` ESM (scratchpad/esm-ts/), including a `declaration: true` library-author scenario and a 9-marker `@ts-expect-error` probe of which types are reachable from the package root.
- Real Jest 30 run against the package, before and after the `default`-condition patch (scratchpad/jesttest/).
- `resolve.exports` condition-matrix simulation across 6 tool profiles (scratchpad/tools/resolve-conditions.mjs).
- Bundling with esbuild 0.25.x (esm/cjs, platform=node, --conditions=node) and `bun build` (--target=node, --format=cjs); minified + gzip byte counts for only-Ok / only-Err / everything / unused-import baselines (scratchpad/bundle/).
- `npm pack` and `bun pm pack --dry-run` into the scratchpad; full tarball inventory.
- `bun x publint` (in-repo, read-only) and `bun x @arethetypeswrong/cli --format table` on the packed tarball (network worked).
- Runtime cross-entrypoint identity/interop probe between the root and /legacy builds (scratchpad/legacy-identity.mjs).
- Acorn parse sweep of every dist/*.js at ecmaVersion 2015→2023 to establish the real runtime floor (scratchpad/tools/ecma.mjs).
- TypeScript LanguageService `getQuickInfoAtPosition` probe to measure what IntelliSense a consumer actually sees for `Ok.map`, `Ok.unwrapOr`, `Ok.flatten`, `Result.try`, `Result.fromPromise`, `Result.do` (scratchpad/tools/quickinfo.mjs).

Not covered (other auditors' dimensions): runtime semantics of Ok/Err/Pending, type-inference correctness of the chaining overloads, the legacy API's internal behavior.

## legacy-migration

Read every file of both generations: core `src/{index,base,ok,err,pending,result,types,errors,utils}.ts` and legacy `src/legacy/{index,result,result-async,chain,types}.ts`; plus `packages/antithrow/{package.json,README.md,CHANGELOG.md}`, the shipped `dist/` and `dist/legacy/` `.d.ts`, the whole `apps/docs/docs/` tree (esp. `docs/legacy/*`, `docs/how-to/core/combine-results.md`, `docs/intro.md`), `packages/eslint-plugin/src/rules/utils/result-type.ts`, and the sibling packages' `package.json`/`CHANGELOG.md`.

Empirical work lives in /tmp/claude-0/-home-user-antithrow/6fa15e49-ae6b-5bd4-b03a-8a3385b537f3/scratchpad/legacy-migration/ (RUN-ALL.sh re-runs everything). Runtime tests via `bun <script>.ts` importing sources by absolute path; type tests via the repo's TypeScript 6.0.3 (`bun x tsc --noEmit --ignoreConfig --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 --skipLibCheck --lib es2022,dom <file>`), using Equal/Expect helpers, `@ts-expect-error` markers, and a `const x: never = expr` trick to force the compiler to print inferred types.

What was covered: (1) full prototype/static feature matrix by runtime reflection (01-matrix.ts); (2) every dropped capability, with a migration snippet actually executed and type-checked (02, 19-cookbook, 21, 22); (3) `Result.all` replacement cost incl. wall-clock concurrency (03); (4) cross-generation interop at runtime and at the type level, both directions, incl. `Result.do` vs `chain` (04, 05, 06, 15); (5) CHANGELOG 3.0.0 claim-by-claim verification incl. deprecation coverage measured with the TS compiler API's suggestion diagnostics (13, 13b, 15); (6) naming drift traps — `fromPromise` contract flip (09, 10), `Result` type-name reuse (12), `mapOrElse` argument-order swap (21); (7) same-package coexistence hazards — identifier collisions (07), bundle size (08), instanceof (04); (8) migration escape-hatch viability against the current ecosystem packages and the ESLint plugin (docsrepro/ecosystem.ts, `bun test packages/eslint-plugin/...`); (9) docs reproduction of the legacy pages' import statements against the real package exports map (docsrepro/).

Not covered (other auditors' dimensions): deep core-only type-inference soundness of `flatten`/variance, `@antithrow/node`/`standard-schema` internals, ESLint rule logic beyond the legacy-exclusion path check.
