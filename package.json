{
	"name": "@antithrow/workspace",
	"private": true,
	"type": "module",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "bun run --filter antithrow build && bun run --filter '!antithrow' build",
		"clean": "bun run --workspaces --if-present clean",
		"lint": "biome check . && bun lint:knip && bun run --workspaces --if-present lint",
		"lint:knip": "knip --cache",
		"format": "biome check --write .",
		"release": "bun run clean && bun run build && changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.12",
		"@changesets/changelog-github": "0.7.0",
		"@changesets/cli": "2.31.0",
		"@types/bun": "1.3.14",
		"knip": "6.25.0",
		"lefthook": "2.1.10",
		"publint": "0.3.21",
		"typescript": "6.0.3"
	}
}
